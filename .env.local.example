# Local overrides for docker-compose variable expansion
# Copy to .env or keep as .env.local (ignored by git) and use the Makefile targets

# Domain model
TENANT_ROOT_DOMAIN=example.com
TENANT_PREFIX=t-

# Provisioner CORS
# Comma-separated list of allowed origins for browser calls
PROVISIONER_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://www.example.com,https://example.com

# Optional: enable extra debug endpoints
# AUTH_DEBUG=1
