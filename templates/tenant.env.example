# Tenant environment template
# These values are substituted by scripts/add-tenant.sh

# Slug of the tenant (used in hostnames), e.g. tenant-aaa
TENANT=tenant-aaa

# Human-friendly tenant name (optional, shown in UI/logs)
TENANT_NAME=

# Base domain for tenants
BASE_DOMAIN_ROOT=example.com

# Prefix for tenant subdomains (t-tenant.example.com)
TENANT_PREFIX=t-

# App container image and tag
APP_IMAGE=ghcr.io/owner/your-app
APP_TAG=latest

# Port exposed by the app container (Traefik routes to this)
APP_PORT=8080

# Host path for persistent app data mounted into the container at /data
# The add-tenant.sh script will substitute the tenant slug in this path.
# Ensure this directory exists on the VM; provision-tenant.sh will create/chown it.
APP_DATA_HOST=/var/lib/anyapp-saas/tenants/tenant-aaa/data

# Optional: Host path for SaaS partial templates (for branding/overrides)
# If unset, defaults to /opt/anyapp-saas/saas-templates on the host.
# For local development, you can point this to the repo directory:
# SAAS_TEMPLATES_HOST_PATH=/Users/you/src/anyapp-saas/saas-templates

# Optional: Logout redirect for app's /logout (Auth0-first chain)
# Clear Auth0 first, then oauth2-proxy, then land on a URL you control.
# 1) In Auth0 → Applications → Your App: add to "Allowed Logout URLs":
#    - https://auth.example.com/oauth2/sign_out
#    - Final landing URL(s), e.g. https://www.example.com (exact match, mind trailing slash)
# 2) Set SF_LOGOUT_REDIRECT_URL to the Auth0-first chain below.
# Example:
# SF_LOGOUT_REDIRECT_URL=https://YOUR_AUTH0_DOMAIN/v2/logout?client_id=YOUR_CLIENT_ID&returnTo=https%3A%2F%2Fauth.example.com%2Foauth2%2Fsign_out%3Frd%3Dhttps%253A%252F%252Fwww.example.com
SF_LOGOUT_REDIRECT_URL=
